{
  "name": "bg-generator",
  "version": "1.0.0",
  "main": "index.html",
  "scripts": {
    "dev": "bun --hot dev.ts",
    "build": "bun build ./src/main.ts --outdir ./dist --minify --target browser && cp index.html styles.css src/shaders/* dist/",
    "preview": "bun --hot dist/index.html"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
